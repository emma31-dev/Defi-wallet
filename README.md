# Defi-wallet

write a solidity contract for a token. token will have a stmbol and a nae. set symbol and name in the contructor of the contract. has a function called transfer to take an address and amount 